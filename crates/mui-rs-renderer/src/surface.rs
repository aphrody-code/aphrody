//! GPU surface — wraps wgpu device/queue/surface + vello renderer.

pub struct RenderSurface;

impl RenderSurface {
    // TODO: pub async fn new(window: &winit::window::Window) -> anyhow::Result<Self>
}

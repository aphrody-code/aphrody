[package]
name = "mui-rs-macros"
description = "Proc-macros for m3-rs — #[m3_component], #[m3_token], derive helpers"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[lib]
proc-macro = true

# The current macros are pass-through stubs that use only the built-in
# `proc_macro` crate. syn/quote/proc-macro2 will return when the macro bodies
# are implemented (#[m3_component] accesskit derive, M3Token conversion).
[dependencies]

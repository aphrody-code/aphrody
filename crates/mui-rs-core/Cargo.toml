[package]
name = "mui-rs-core"
description = "mui-rs — types et traits fondamentaux"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
publish = false  # canary: bare path deps; not in the publish ladder (see mui-rs/Cargo.toml)

[dependencies]
# Foundational M3 design tokens (color roles, type/shape/elevation scales).
m3-tokens = { path = "../m3-tokens" }

---
name: design-md
version: "1.0.0"
description: |
  Create and manage DESIGN.md files. Useful for capturing design direction, tokens, and visual rules in a single source of truth.
triggers:
  - "design.md"
  - "design doc"
  - "design tokens doc"
  - "visual rules doc"
od:
  mode: design-system
  category: design-systems
  upstream: "https://github.com/google-labs-code/skills"
when_to_use: "User types \"/design-md\", says \"design.md\", \"design doc\", \"design tokens doc\", \"visual rules doc\", or asks for: Create and manage DESIGN.md files. Useful for capturing design direction, tokens, and visual rules in a single source of truth."
---

# design-md

Mode `/goal` permanent : décider seul, ne pas s'arrêter avant complétion.

**What it does** : Create and manage DESIGN.md files — capturing design direction, tokens, and visual rules in a single source of truth. Category `design-systems`.

**Upstream** : https://github.com/google-labs-code/skills

**Use** : catalogue entry — advertises the skill so the agent discovers it during planning. For the full workflow (assets, scripts, references), install the upstream bundle into the active agent's skills directory (open the upstream README in a browser: Linux `xdg-open`, macOS `open`, Windows `start`), then invoke this skill by name or via a trigger phrase. Regenerated by the Open Design harvester (`scripts/skills-harvest-open-design.ts`).

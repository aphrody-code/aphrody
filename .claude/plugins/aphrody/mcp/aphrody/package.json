{
  "name": "@aphrody-code/aphrody-mcp",
  "version": "0.1.0",
  "description": "Unified MCP stdio server for the aphrody Claude Code plugin — fuses scraping (bxc-mcp Rust subprocess), memory/vision (Bun), and aphrody CLI wrappers (exec) into one tool surface.",
  "type": "module",
  "bin": {
    "aphrody-mcp": "./server/index.ts"
  },
  "main": "./server/index.ts",
  "scripts": {
    "dev": "bun run server/index.ts",
    "build": "bun build server/index.ts --outdir=dist --target=node --format=esm",
    "list-tools": "bun run server/index.ts --list-tools",
    "pack": "npx @anthropic-ai/mcpb pack ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.23.0"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "license": "Apache-2.0",
  "author": "aphrody-code <37252373+aphrody-code@users.noreply.github.com>"
}

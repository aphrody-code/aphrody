{
  "name": "@aphrody-code/mcp-bxc-scrapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP stdio server that exposes the bxc (Bun+Lightpanda) scraping engine as 7 tools for Claude Code. Backed by the bxc daemon over HTTP or by spawning bxc-engine directly when the daemon is unavailable.",
  "license": "Apache-2.0",
  "author": "aphrody-code <contact@aphrody-code.dev>",
  "homepage": "https://github.com/aphrody-code/aphrody",
  "main": "./server.ts",
  "bin": {
    "aphrody-mcp-bxc-scrapper": "./server.ts"
  },
  "scripts": {
    "start": "bun run server.ts",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx oxlint --quiet"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}

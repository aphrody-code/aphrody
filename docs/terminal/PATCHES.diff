diff --git a/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake b/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
index 37b85c3e0..796469931 100644
--- a/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/arm64-windows-static.cmake
@@ -3,6 +3,6 @@ set(VCPKG_TARGET_ARCHITECTURE arm64)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 to match VS 2026 Insiders (the only
+# toolset present on this host). See docs/terminal/BUILD.md for rationale.
+set(VCPKG_PLATFORM_TOOLSET v145)
diff --git a/dep/vcpkg-overlay-triplets/fuzzing/arm64-windows-static.cmake b/dep/vcpkg-overlay-triplets/fuzzing/arm64-windows-static.cmake
index 9f2c8d0a1..2e1628300 100644
--- a/dep/vcpkg-overlay-triplets/fuzzing/arm64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/fuzzing/arm64-windows-static.cmake
@@ -3,9 +3,8 @@ set(VCPKG_TARGET_ARCHITECTURE arm64)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 (VS 2026). See docs/terminal/BUILD.md.
+set(VCPKG_PLATFORM_TOOLSET v145)

 set(VCPKG_CXX_FLAGS /fsanitize=address)
 set(VCPKG_C_FLAGS /fsanitize=address)
diff --git a/dep/vcpkg-overlay-triplets/fuzzing/x64-windows-static.cmake b/dep/vcpkg-overlay-triplets/fuzzing/x64-windows-static.cmake
index 7b3aeea85..711074e4a 100644
--- a/dep/vcpkg-overlay-triplets/fuzzing/x64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/fuzzing/x64-windows-static.cmake
@@ -3,9 +3,8 @@ set(VCPKG_TARGET_ARCHITECTURE x64)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 (VS 2026). See docs/terminal/BUILD.md.
+set(VCPKG_PLATFORM_TOOLSET v145)

 set(VCPKG_CXX_FLAGS /fsanitize=address)
 set(VCPKG_C_FLAGS /fsanitize=address)
diff --git a/dep/vcpkg-overlay-triplets/fuzzing/x86-windows-static.cmake b/dep/vcpkg-overlay-triplets/fuzzing/x86-windows-static.cmake
index 9e6a72cdf..6871c5e27 100644
--- a/dep/vcpkg-overlay-triplets/fuzzing/x86-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/fuzzing/x86-windows-static.cmake
@@ -3,9 +3,8 @@ set(VCPKG_TARGET_ARCHITECTURE x86)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 (VS 2026). See docs/terminal/BUILD.md.
+set(VCPKG_PLATFORM_TOOLSET v145)

 set(VCPKG_CXX_FLAGS /fsanitize=address)
 set(VCPKG_C_FLAGS /fsanitize=address)
diff --git a/dep/vcpkg-overlay-triplets/x64-windows-static.cmake b/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
index 12e0256dd..213c4de71 100644
--- a/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/x64-windows-static.cmake
@@ -3,6 +3,6 @@ set(VCPKG_TARGET_ARCHITECTURE x64)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 to match VS 2026 Insiders (the only
+# toolset present on this host). See docs/terminal/BUILD.md for rationale.
+set(VCPKG_PLATFORM_TOOLSET v145)
diff --git a/dep/vcpkg-overlay-triplets/x86-windows-static.cmake b/dep/vcpkg-overlay-triplets/x86-windows-static.cmake
index 45869c1dd..9737b3495 100644
--- a/dep/vcpkg-overlay-triplets/x86-windows-static.cmake
+++ b/dep/vcpkg-overlay-triplets/x86-windows-static.cmake
@@ -3,6 +3,6 @@ set(VCPKG_TARGET_ARCHITECTURE x86)
 set(VCPKG_CRT_LINKAGE static)
 set(VCPKG_LIBRARY_LINKAGE static)

-# ...but with explicit platform toolset, so that future toolsets
-# aren't automatically picked up (it defaults to the latest one).
-set(VCPKG_PLATFORM_TOOLSET v143)
+# Local patch (aphrody): v143 -> v145 to match VS 2026 Insiders (the only
+# toolset present on this host). See docs/terminal/BUILD.md for rationale.
+set(VCPKG_PLATFORM_TOOLSET v145)
diff --git a/src/common.build.pre.props b/src/common.build.pre.props
index b3f57bf23..954099d3b 100644
--- a/src/common.build.pre.props
+++ b/src/common.build.pre.props
@@ -135,8 +135,11 @@
           This diagnostic is broken in VS 17.7 which our CI currently uses. It's fixed in 17.8.
         C26494: Variable 'index' is uninitialized. Always initialize an object (type. 5).
           This diagnostic is broken in VS 17.7 which our CI currently uses. It's fixed in 17.8.
+        C4875 (aphrody local patch): non-string-literal argument to [[gsl::suppress]] is deprecated.
+          ms-gsl 3.1.0 (pinned in vcpkg.json) still uses this old syntax; MSVC v145 (VS 2026)
+          emits C4875 for every GSL_SUPPRESS macro. Until ms-gsl is bumped upstream, silence.
       -->
-      <DisableSpecificWarnings>4201;4312;4467;5105;26434;26445;26456;26478;26494;%(DisableSpecificWarnings)</DisableSpecificWarnings>
+      <DisableSpecificWarnings>4201;4312;4467;4875;5105;26434;26445;26456;26478;26494;%(DisableSpecificWarnings)</DisableSpecificWarnings>
       <PreprocessorDefinitions>_WINDOWS;EXTERNAL_BUILD;_SILENCE_STDEXT_ARR_ITERS_DEPRECATION_WARNING;%(PreprocessorDefinitions)</PreprocessorDefinitions>
       <SDLCheck>true</SDLCheck>
       <PrecompiledHeaderFile>precomp.h</PrecompiledHeaderFile>
diff --git a/src/wap-common.build.pre.props b/src/wap-common.build.pre.props
index f99f1e164..ce6406571 100644
--- a/src/wap-common.build.pre.props
+++ b/src/wap-common.build.pre.props
@@ -63,7 +63,11 @@
   <Import Project="$(WapProjPath)\Microsoft.DesktopBridge.props" />

   <PropertyGroup>
-    <TargetPlatformVersion>10.0.22621.0</TargetPlatformVersion>
+    <!-- Local patch (aphrody): make TargetPlatformVersion overridable so
+         we can build with Windows SDK 10.0.26100.0 (the only SDK on this
+         host) instead of the hard-coded 22621. See docs/terminal/PATCHES.diff
+         and docs/terminal/BUILD.md. -->
+    <TargetPlatformVersion Condition="'$(TargetPlatformVersion)' == ''">10.0.22621.0</TargetPlatformVersion>
     <TargetPlatformMinVersion>10.0.18362.0</TargetPlatformMinVersion>
     <DefaultLanguage>en-US</DefaultLanguage>
   </PropertyGroup>
